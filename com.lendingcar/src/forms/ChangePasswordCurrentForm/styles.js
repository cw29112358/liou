import { objectMerge } from 'utils/helpers';
import formStyles from 'forms/styles';

export default {
  ...objectMerge(formStyles,
    {
      buttonCircle: {
        marginTop: 111.5,
      },
    }),
};
