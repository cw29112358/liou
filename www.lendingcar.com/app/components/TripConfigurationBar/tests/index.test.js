// import React from 'react';
// import { shallow } from 'enzyme';

// import TripConfigurationForm from '../index';

describe('<TripConfigurationForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
