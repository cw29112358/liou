// import { fromJS } from 'immutable';
// import { makeSelectBookingPageDomain } from '../selectors';

// const selector = makeSelectBookingPageDomain();

describe('makeSelectBookingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
