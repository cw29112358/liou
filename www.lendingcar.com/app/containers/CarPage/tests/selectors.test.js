// import { fromJS } from 'immutable';
// import { selectCarPageDomain } from '../selectors';

// const selector = selectCarPageDomain();

describe('selectCarPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
